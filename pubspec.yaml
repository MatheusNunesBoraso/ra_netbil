name: ra_netbil
description: "Prototipo de Realidade Aumentada - escaneia capa de livro e exibe modelo 3D."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  arcore_flutter_plugin: ^0.1.0
  arkit_plugin: ^1.1.0
  ar_flutter_plugin_2: ^0.0.3
  flutter_inappwebview: ^6.1.5
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.3
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/models/
    - assets/mindar/
